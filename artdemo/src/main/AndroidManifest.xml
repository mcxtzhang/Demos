<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="anlaiye.com.cn.artdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".c1.LifeCycleTestActivity"
            android:taskAffinity="aaa.bbb.ccc" />
        <activity
            android:name=".c1.LifeCycleTest2Activity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".lifecycle.AActivity"
            android:launchMode="singleTop" />
        <activity android:name=".lifecycle.BActivity" />
        <activity android:name=".lifecycle.CActivity" />
        <activity android:name=".lifecycle.DActivity" />
        <activity android:name=".forresult.ForResultActivity1" />
        <activity android:name=".forresult.ForResultActivity2" />
        <activity android:name=".forresult.ForResultActivity3" />
        <activity android:name=".createfinish.CreateFinishActivity" />
        <activity android:name=".rotatesaveinstance.RorateSaveinstanceActivity"></activity>
    </application>

</manifest>