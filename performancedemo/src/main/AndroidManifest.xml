<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="anlaiye.com.cn.performancedemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".monitor.framemetrics.demo.CstApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".measure.TestMeasureActivity" />
        <activity android:name=".measure.TestMeasureRelativeActivity" />
        <activity android:name=".measure.TestMeasureLInearActivity" />
        <activity android:name=".monitor.MonitorActivity">

        </activity>
        <activity android:name=".monitor.list.RvActivity"></activity>
        <activity android:name=".merge.TestMergeParentActivity"><intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
    </application>

</manifest>