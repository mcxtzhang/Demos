<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="mcxtzhang.recyclerviewdemo">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".generalizerv.BaseRecyclerViewActivity">
        </activity>
        <activity android:name=".nestrv.ViewPagerNestRvActivity">
        </activity>
        <activity android:name=".anyview.AnyViewActivity">
        </activity>
        <activity android:name=".itemtouchhelper.TestItemHelperActivity">
        </activity>
        <activity android:name=".expand.ExpandRvActivity">
        </activity>
        <activity android:name=".PhotoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>